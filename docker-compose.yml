version: '3.8'

services:
  a365ss-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: a365ss_react_app
    ports:
      - "8082:80" # Maps port 8082 on the server to port 80 in the container
    restart: unless-stopped
