version: '3.8'

services:
  a365ss-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: a365ss_react_app
    ports:
      - "8084:80" # React app on 8084 — Angular nginx proxies /a365ss to here
    restart: unless-stopped
