version: '3.8'

services:
  a365ss-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: a365ss_react_app
    ports:
      - "80:80"   # Maps port 80 on the server to port 80 in the container
    restart: unless-stopped
